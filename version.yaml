run: true
version: '5.33.dev3'
description: |
  - Added `hungerbridge.url` configuration option to specify the URL of the HungerBridge API.
  - Updated default configuration to include the new `hungerbridge.url` option.
  - Updated `Config` class to include the new `bridge_url` attribute.
  - Updated `Watcher` class to use the new `bridge_url` configuration option when initializing the `HungerBridgeClient`.